{
  "name": "@crexi-dev/lint",
  "version": "1.0.2",
  "description": "ts lint rules for crexi",
  "main": "tslint.json",
  "scripts": {
    "build": "tsc --outDir ./dist/lint",
    "postbuild": "npx copy {package,tslint}.json ./dist/lint/.",
    "clean": "rimraf ./dist/lint",
    "dist": "npm run clean && npm run build",
    "test": "exit"
  },
  "author": "Neil Soult",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^12.11.1",
    "rimraf": "^2.6.3",
    "tslint": "^5.19.0",
    "tsutils": "^3.17.1",
    "typescript": "~3.4.5"
  }
}
